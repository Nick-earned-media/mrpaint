# Copy to .env.local for local script use. NEVER commit .env.local.
#
# Production values live in Vercel — set via `vercel env add NAME production`.

# ─── Supabase ─────────────────────────────────────────────────────────────
SUPABASE_URL="https://nisblrrjgnjcjxxmenjk.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="paste_service_role_key_here"

# ─── OpenAI (embeddings only — text-embedding-3-small) ────────────────────
OPENAI_API_KEY="sk-..."

# ─── Anthropic (Claude — already set on Vercel) ──────────────────────────
ANTHROPIC_API_KEY="sk-ant-..."

# ─── Twilio (already set on Vercel) ──────────────────────────────────────
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
TWILIO_FROM="whatsapp:+14155238886"
ALLOWED_PHONES=""

# ─── GitHub deploys (already set on Vercel) ──────────────────────────────
GITHUB_REPO="Nick-earned-media/mrpaint"
GITHUB_TOKEN=""

# ─── Semrush (already set on Vercel) ─────────────────────────────────────
SEMRUSH_API_KEY=""
SEMRUSH_PROJECT_ID="29844457"
